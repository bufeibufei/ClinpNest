<Window x:Class="ClipNest.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="设置"
        Height="390"
        Width="480"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="设置" FontSize="24" FontWeight="SemiBold"/>
        <TextBlock Grid.Row="1"
                   Text="调整快捷键和剪切板历史容量。"
                   Foreground="{StaticResource TextSecondaryBrush}"
                   Margin="0,8,0,22"/>

        <StackPanel Grid.Row="2">
            <TextBlock Text="唤起快捷面板" Margin="0,0,0,8"/>
            <TextBox x:Name="HotkeyBox" IsReadOnly="True" PreviewKeyDown="HotkeyBox_PreviewKeyDown"/>
            <TextBlock x:Name="HintText" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,8,0,18"/>

            <TextBlock Text="历史条目存储上限" Margin="0,0,0,8"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="56"/>
                </Grid.ColumnDefinitions>
                <Slider x:Name="HistoryLimitSlider"
                        Minimum="20"
                        Maximum="500"
                        TickFrequency="20"
                        IsSnapToTickEnabled="True"
                        ValueChanged="HistoryLimitSlider_ValueChanged"/>
                <TextBox x:Name="HistoryLimitBox"
                         Grid.Column="1"
                         Margin="10,0,0,0"
                         TextChanged="HistoryLimitBox_TextChanged"/>
            </Grid>
            <TextBlock Text="默认 100。收藏条目不会因为达到上限而被自动删除。"
                       Foreground="{StaticResource TextSecondaryBrush}"
                       Margin="0,8,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="取消" Margin="0,0,8,0" Click="CancelButton_Click"/>
            <Button Content="保存" Background="#2563EB" Foreground="White" BorderBrush="#2563EB" Click="SaveButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
